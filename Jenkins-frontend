pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {

    stage('Build') {
      steps {
        dir("frontend") {
            sh "pwd"
            sh 'npm install'
            sh 'npm run build'
        }  
      }
    }
    
    stage('Start') {
      steps {
        dir("frontend") {
            sh "pwd"
            sh 'npm run start-prod'
        }  
      }
    }

  }
}