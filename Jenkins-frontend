pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {

    stage('Build') {
      steps {
        dir("frontend") {
            sh 'npm i core-js@2.6.9'
            sh 'npm install'
            sh 'npm run build'
        }  
      }
    }
    
    stage('Start') {
      steps {
        dir("frontend") {
            sh 'npm run start-prod'
        }  
      }
    }

  }
}
