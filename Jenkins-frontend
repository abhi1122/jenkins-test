pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {

    stage('Build') {
      steps {
        dir("frontend") {
            sh 'yarn'
            sh 'yarn run build'
        }  
      }
    }
    
    stage('Start') {
      steps {
        dir("frontend") {
            sh 'yarn run start-prod'
        }  
      }
    }

  }
}
