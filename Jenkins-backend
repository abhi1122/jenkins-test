pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {

    stage('Start') {
      steps {
        sh "pwd"
        sh 'npm install --g cross-env'
        sh 'npm install -g jest'
        sh 'npm install pm2 -g' 
      }
    }
  
    stage('Build') {
      steps {
        dir("backend") {
            sh 'npm install'
        }  
      }
    }
    
   stage('Test') {
      steps {
        dir("backend") {
            sh 'npm test'
        }  
      }
    }
    
    stage('Run') {
      steps {
        dir("backend") {
            sh 'pm2 kill'
            sh "BUILD_ID=dontKillMe npm start"
            sh 'pm2 ls'
            sh 'pm2 list'
        }  
      }
    }

  }
}
