pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {
  
    stage('Build') {
      steps {
        dir("backend") {
            sh 'npm install'
        }  
      }
    }
    
    stage('Run') {
      steps {
        dir("backend") {
            
            sh "export BUILD_ID=$BUILD_ID"
            sh 'JENKINS_NODE_COOKIE=dontKillMe npm run start:prod'
            sh "pm2 list"
        }  
      }
    }

  }
}
