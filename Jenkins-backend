pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {
  
    stage('Build') {
      steps {
        dir("backend") {
            sh 'npm install'
        }  
      }
    }
    
    stage('Run') {
      steps {
        dir("backend") {
            sh 'pm2 kill'
            sh "npm start"
            sh 'pm2 list'
            sh 'pm2 ping'
        }  
      }
    }

  }
}
