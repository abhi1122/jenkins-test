pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {

    stage('Start') {
      steps {
        bat 'npm install --g cross-env'
        bat 'npm install -g jest'
        bat 'npm install pm2 -g' 
      }
    }

  }
}
