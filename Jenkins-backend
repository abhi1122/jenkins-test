pipeline {
  agent any
  tools {nodejs "nodejs"}  
  stages {
  
    stage('Build') {
      steps {
        dir("backend") {
            sh 'npm install'
        }  
      }
    }
    
    stage('Run') {
      steps {
        dir("backend") {
            sh 'pm2 kill'
            sh "pm2 start ./bin/www"
            sh 'pm2 list'
            sh 'pm2 ping'
        }  
      }
    }

  }
}
